apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: ui-rollout
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 50
        - pause: {duration: "4m"}  # Manual intervention here
        - setWeight: 100
  selector:
    matchLabels:
      app: ui
  template:
    metadata:
      labels:
        app: ui
    spec:
      containers:
        - name: ui-container
          image: lexmarkkubernetes/uiservice-dev:51adafc89515038407acd226e46ef4b6ede4422b
          imagePullPolicy: Always
          ports:
            - containerPort: 80
