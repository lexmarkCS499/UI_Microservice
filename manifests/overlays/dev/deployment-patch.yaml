# k8s/overlays/dev/deployment-patch.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui-app
spec:
  template:
    spec:
      containers:
        - name: ui-app
          image: docker.io/lexmarkkubernetes/uiservice:dev # Example of using a dev image tag
